# Optional public base path for static hosting.
# Use "/" when serving the built dist directory from the domain root.
# Use "/calculator/" or another leading/trailing-slash path for subdirectory hosting.
VITE_BASE_PATH=/
